groups:
- name: example
  rules:
  - alert: High CPU
    expr: max(mesos_slave_cpus_used) > 8
    for: 10m
    labels:
      severity: email
      body: High CPU Utilization Detected 
    annotations:
      summary: High CPU Detected
